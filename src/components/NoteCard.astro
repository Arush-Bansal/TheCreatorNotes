---
interface Props {
  title: string;
  description: string;
  category: string;
  href: string;
  icon?: string;
  color?: string;
}

const { title, description, category, href, icon = "📝", color = "var(--accent-red)" } = Astro.props;
---

<a href={href} class="note-card box-border shadow-offset">
  <div class="card-tag" style={`background-color: ${color}`}>
    {category}
  </div>
  
  <div class="icon-circle" style={`border-color: ${color}`}>
    <span class="icon">{icon}</span>
  </div>

  <h3 class="card-title">{title}</h3>
  <p class="card-description">{description}</p>
  
  <div class="card-footer">
    <span class="read-more">Read Full Note</span>
    <span class="arrow">→</span>
  </div>
</a>

<style>
  .note-card {
    display: flex;
    flex-direction: column;
    padding: 2.5rem 2rem 2rem;
    position: relative;
    height: 100%;
    transition: transform 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .card-tag {
    position: absolute;
    top: -12px;
    left: 20px;
    padding: 0.2rem 0.8rem;
    border: var(--border-width) solid var(--border-color);
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    color: white;
    border-radius: var(--radius-sm);
  }

  .icon-circle {
    width: 60px;
    height: 60px;
    border: var(--border-width) solid var(--border-color);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: white;
    margin-bottom: 1.5rem;
    font-size: 1.8rem;
  }

  .card-title {
    font-size: 1.4rem;
    margin-bottom: 0.75rem;
    color: var(--text-primary);
  }

  .card-description {
    font-size: 0.95rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    flex-grow: 1;
  }

  .card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1rem;
    border-top: var(--border-width) solid rgba(0,0,0,0.05);
  }

  .read-more {
    font-weight: 800;
    font-size: 0.85rem;
    text-transform: uppercase;
  }

  .arrow {
    font-size: 1.2rem;
    transition: transform 0.2s ease;
  }

  .note-card:hover .arrow {
    transform: translateX(5px);
  }
</style>
